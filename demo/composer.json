{
    "name": "keyboardman/filesystem-bundle-demo",
    "description": "Démo Symfony utilisant keyboardman/filesystem-bundle",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "keyboardman/filesystem-bundle": "@dev"
    },
    "repositories": [
        {
            "type": "path",
            "url": ".."
        }
    ],
    "minimum-stability": "dev",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "allow-plugins": {}
    }
}
